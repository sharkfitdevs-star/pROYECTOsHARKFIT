// Setup file for Vitest + Testing Library
import '@testing-library/jest-dom'

// If you need global polyfills for fetch in Node+jsdom, add them here.
// e.g. globalThis.fetch = globalThis.fetch || (await import('node-fetch')).default
