{
  "name": "Proyeccion_Configuracion",
  "type": "object",
  "properties": {
    "sede": {
      "type": "string",
      "description": "ID de la sede (relación a Sucursales)"
    },
    "mes": {
      "type": "string",
      "description": "Mes de la proyección en formato YYYY-MM (ej: 2024-01)"
    },
    "presupuesto_mensual": {
      "type": "number",
      "description": "Presupuesto publicitario mensual configurado manualmente (opcional, si no se especifica se calcula automáticamente)"
    },
    "costo_por_lead": {
      "type": "number",
      "description": "Costo por lead configurado manualmente (opcional, si no se especifica se calcula automáticamente)"
    },
    "porcentaje_agendamiento": {
      "type": "number",
      "description": "Porcentaje de agendamiento configurado manualmente (0-100, opcional)"
    },
    "porcentaje_asistencia": {
      "type": "number",
      "description": "Porcentaje de asistencia configurado manualmente (0-100, opcional)"
    },
    "porcentaje_conversion": {
      "type": "number",
      "description": "Porcentaje de conversión configurado manualmente (0-100, opcional)"
    },
    "ticket_promedio": {
      "type": "number",
      "description": "Ticket promedio configurado manualmente (opcional, si no se especifica se calcula automáticamente)"
    },
    "activo": {
      "type": "boolean",
      "default": true,
      "description": "Indica si esta configuración está activa"
    },
    "notas": {
      "type": "string",
      "description": "Notas adicionales sobre la configuración"
    }
  },
  "required": ["sede", "mes"]
}