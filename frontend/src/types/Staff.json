{
  "name": "Staff",
  "type": "object",
  "properties": {
    "nombre": {
      "type": "string",
      "description": "Nombre completo del staff"
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "Email del staff (opcional)"
    },
    "sede_principal": {
      "type": "string",
      "description": "ID de la sede principal del staff (relación a Sucursales)"
    },
    "roles": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["vendedor", "asistente", "RS", "jefe_ventas", "direccion"]
      },
      "description": "Roles del staff (multiopción): vendedor, asistente, RS (Responsable de Sede), jefe_ventas, direccion"
    },
    "activo": {
      "type": "boolean",
      "default": true,
      "description": "Indica si el staff está activo"
    },
    "rol_personalizado": {
      "type": "string",
      "description": "ID del rol personalizado asignado (relación a Roles)"
    }
  },
  "required": ["nombre", "roles"]
}