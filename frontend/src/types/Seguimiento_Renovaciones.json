{
  "name": "Seguimiento_Renovaciones",
  "type": "object",
  "properties": {
    "cliente_id": {
      "type": "string",
      "description": "ID del cliente (relación a Clientes)"
    },
    "fecha_contacto": {
      "type": "string",
      "format": "date",
      "description": "Fecha en que se contactó al cliente"
    },
    "contactado": {
      "type": "boolean",
      "description": "Si el cliente fue contactado (Sí/No)"
    },
    "renovado": {
      "type": "boolean",
      "default": false,
      "description": "Si el cliente renovó después del contacto"
    },
    "razon_no_renovacion": {
      "type": "string",
      "description": "Razón por la cual el cliente no renovó (opcional)"
    },
    "detalle_contacto": {
      "type": "string",
      "description": "Detalles adicionales del contacto realizado"
    },
    "responsable": {
      "type": "string",
      "description": "ID del staff que realizó el contacto (relación a Staff)"
    },
    "sede": {
      "type": "string",
      "description": "ID de la sede del cliente (relación a Sucursales)"
    }
  },
  "required": ["cliente_id", "fecha_contacto", "contactado"]
}