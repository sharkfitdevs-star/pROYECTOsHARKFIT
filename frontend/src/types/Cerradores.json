{
  "name": "Cerradores",
  "type": "object",
  "properties": {
    "nombre_cerrador": {
      "type": "string",
      "description": "Nombre del cerrador"
    },
    "staff_relacionado": {
      "type": "string",
      "description": "ID del staff relacionado (relación opcional a Staff)"
    },
    "activo": {
      "type": "boolean",
      "default": true,
      "description": "Indica si el cerrador está activo"
    }
  },
  "required": ["nombre_cerrador"]
}