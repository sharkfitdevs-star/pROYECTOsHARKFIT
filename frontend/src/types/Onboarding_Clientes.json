{
  "name": "Onboarding_Clientes",
  "type": "object",
  "properties": {
    "cliente_id": {
      "type": "string",
      "description": "ID del cliente (relación a Clientes)"
    },
    "cliente_nombre": {
      "type": "string",
      "description": "Nombre del cliente (desnormalizado)"
    },
    "cliente_whatsapp": {
      "type": "string",
      "description": "WhatsApp del cliente (desnormalizado)"
    },
    "sede": {
      "type": "string",
      "description": "ID de la sede (relación a Sucursales)"
    },
    "fecha_ingreso": {
      "type": "string",
      "format": "date-time",
      "description": "Fecha y hora en que el cliente fue creado"
    },
    "contrato_firmado": {
      "type": "boolean",
      "default": false,
      "description": "Indica si el cliente firmó el contrato"
    },
    "fecha_firma_contrato": {
      "type": "string",
      "format": "date-time",
      "description": "Fecha y hora en que se firmó el contrato"
    },
    "tarjeta_registrada": {
      "type": "boolean",
      "default": false,
      "description": "Indica si el cliente registró su tarjeta"
    },
    "fecha_registro_tarjeta": {
      "type": "string",
      "format": "date-time",
      "description": "Fecha y hora en que se registró la tarjeta"
    },
    "plan_no_requiere_tarjeta": {
      "type": "boolean",
      "default": false,
      "description": "Indica si el plan del cliente no requiere tarjeta (ej: planes prepago)"
    },
    "estado_onboarding": {
      "type": "string",
      "enum": ["Pendiente", "En Proceso", "Completado"],
      "default": "Pendiente",
      "description": "Estado del proceso de onboarding"
    },
    "responsable_asignado": {
      "type": "string",
      "description": "ID del staff responsable del onboarding (relación a Staff)"
    },
    "tarea_contrato_id": {
      "type": "string",
      "description": "ID de la tarea de contrato en Tareas_RS (si existe)"
    },
    "tarea_tarjeta_id": {
      "type": "string",
      "description": "ID de la tarea de tarjeta en Tareas_RS (si existe)"
    },
    "notas": {
      "type": "string",
      "description": "Notas adicionales del proceso de onboarding"
    },
    "plataforma_tarjeta": {
      "type": "string",
      "enum": ["EVO", "Mercado Pago"],
      "description": "Plataforma donde se registró la tarjeta"
    },
    "correo_mercado_pago": {
      "type": "string",
      "description": "Correo de registro en Mercado Pago (si aplica)"
    }
  },
  "required": ["cliente_id", "sede", "fecha_ingreso"]
}