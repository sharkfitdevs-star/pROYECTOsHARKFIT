{
  "name": "Planes_Servicios",
  "type": "object",
  "properties": {
    "nombre_plan": {
      "type": "string",
      "description": "Nombre único del plan o servicio"
    },
    "tipo_item": {
      "type": "string",
      "enum": ["Plan", "Servicio", "Programa"],
      "description": "Tipo de item: Plan, Servicio o Programa"
    },
    "duracion_meses": {
      "type": "number",
      "description": "Duración en meses del plan (1, 6, 12, etc.)"
    },
    "modalidad_cobro": {
      "type": "string",
      "enum": ["Suscripción", "Prepago"],
      "description": "Modalidad de cobro: Suscripción (recurrente) o Prepago (pago único)"
    },
    "precio": {
      "type": "number",
      "description": "Precio del plan o servicio (opcional)"
    },
    "activo": {
      "type": "boolean",
      "default": true,
      "description": "Indica si el plan está activo"
    }
  },
  "required": ["nombre_plan", "tipo_item", "duracion_meses", "modalidad_cobro"]
}